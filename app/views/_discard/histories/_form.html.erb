<%= form_with(model: history, local: true) do |form| %>
  <% if history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(history.errors.count, "error") %> prohibited this history from being saved:</h2>

      <ul>
      <% history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cash %>
    <%= form.text_field :cash, id: :history_cash %>
  </div>

  <div class="field">
    <%= form.label :stocks %>
    <%= form.text_field :stocks, id: :history_stocks %>
  </div>

  <div class="field">
    <%= form.label :stocks_count %>
    <%= form.number_field :stocks_count, id: :history_stocks_count %>
  </div>

  <div class="field">
    <%= form.label :options %>
    <%= form.text_field :options, id: :history_options %>
  </div>

  <div class="field">
    <%= form.label :options_count %>
    <%= form.text_field :options_count, id: :history_options_count %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total, id: :history_total %>
  </div>

  <div class="field">
    <%= form.label :snapshot_date %>
    <%= form.datetime_select :snapshot_date, id: :history_snapshot_date %>
  </div>

  <div class="field">
    <%= form.label :portfolio_id %>
    <%= form.number_field :portfolio_id, id: :history_portfolio_id %>
  </div>

  <div class="field">
    <%= form.label :daily_dividend %>
    <%= form.text_field :daily_dividend, id: :history_daily_dividend %>
  </div>

  <div class="field">
    <%= form.label :daily_dividend_date %>
    <%= form.datetime_select :daily_dividend_date, id: :history_daily_dividend_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
